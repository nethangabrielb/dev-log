// session type
export enum SessionType {
  PROJECT = "Project",
  ARTICLE = "Article",
  DSA = "DSA Problem",
  STUDY = "Study",
  OTHER = "Other",
}

// session linked to kind
export enum LinkedToKind {
  PROJECT = "Project",
}
