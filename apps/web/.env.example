# Base URL of the deployed backend API. Used by the Axios client and the Google OAuth redirect.
# Dev default: http://localhost:3000
# Production: set in .env.production, e.g. https://api.devlog.example.com
VITE_API_URL=http://localhost:3000
